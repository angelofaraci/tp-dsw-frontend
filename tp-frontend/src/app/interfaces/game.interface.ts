export interface Game {
    _id?: any,
    id?: string,
    name?: string,
    description?: string,
    cover?: string,
    banner?: string,
    release_date?: string,
    website?: string,
    socials?: string[],
    rating: number
}